[project]
name = "topology-gomoku-tooling"
version = "0.1.0"
description = "Python tooling environment for topology-gomoku"
requires-python = ">=3.12,<3.13"
dependencies = [
  "brotli>=1.1,<2",
  "fonttools>=4.55,<5",
  "kokoro-onnx>=0.6.1",
  "qwen-tts==0.1.1",
  "soundfile>=0.14.0",
]

[tool.uv]
package = false
