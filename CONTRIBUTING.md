# 贡献指南

感谢你愿意让拓扑五子棋变得更有趣。玩法、数学规则、视觉、声音、性能、无障碍、测试、文档和平台适配都属于有价值的贡献。

## 开始之前

1. 先确认改动属于共享玩法还是某个平台的原生适配。
2. 从正确的基线创建短期分支：共享改动从 `dev` 开始，平台专属改动从对应发行分支开始。
3. 在提交前运行与改动相称的测试，并记录人工验证过的设备、视口或宿主环境。
4. 不提交账号、密钥、签名文件、平台私有配置或本地构建 ZIP。

如果一个改动同时涉及共享玩法和平台桥接，请尽量拆成两个可独立审查的提交或 Pull Request。

## 分支模型

| 分支 | 用途 | 接受的改动 |
| --- | --- | --- |
| `dev` | 日常集成与下一版本开发 | 共享玩法、测试、设计、文档、可复用的平台抽象 |
| `main` | 稳定、可发布的跨平台基线 | 从 `dev` 提升的已验证改动；紧急修复除外不直接开发 |
| `xiaohongshu` | 小红书发行版 | 小红书原生适配、配置、打包和发布修复 |
| `bilibili` | B 站 Toy 发行版 | Toy API、生命周期、资源和发布配置 |
| `wechat` | 微信小程序发行版 | 小程序生命周期、组件、资源和发布配置 |

共享能力的常规流向是 `短期分支 → dev → main → 三个发行分支`。平台分支中发现的通用修复应先提炼回 `dev`，再沿正常路径传播，避免三个版本长期漂移。

完整策略见 [docs/REPOSITORY.md](docs/REPOSITORY.md)。

## Agent-first 协作

我们欢迎使用 Codex 或其他 Agent 参与开发。每个 AI 辅助提交都应满足这些要求：

- 人类提交者对代码、素材授权、测试结果与外部副作用负责。
- Agent 开始工作前读取 `AGENTS.md`，确认当前分支和 worktree，不在错误的发行分支上混入平台代码。
- Prompt 不是验证证据；Pull Request 应列出实际运行的命令和人工检查。
- 不把聊天记录、思维过程、访问令牌或本机绝对路径提交到仓库。
- 大改动写清楚决策、约束与未完成项，让下一位协作者无需猜测上下文。

建议在 Pull Request 描述中留下简短交接：

```text
目标：
范围：共享 / 小红书 / B 站 / 微信
验证：
视觉或真机检查：
已知限制：
```

## 验证

共享代码至少运行：

```powershell
npm test
npm run validate
```

小红书发布改动还应运行：

```powershell
npm run build:xiaohongshu
```

平台原生适配建立后，各发行分支应在自己的平台模拟器和至少一台真机上补充验收记录。涉及视觉变化时，请提供同尺寸的前后对比；涉及拓扑规则时，请补充确定性测试。

## 提交风格

使用简洁的 Conventional Commit 前缀：

- `feat:` 新玩法或用户可见能力
- `fix:` 缺陷修复
- `refactor:` 不改变行为的结构调整
- `test:` 测试与验证工具
- `docs:` 文档
- `build:` 构建、平台配置或依赖
- `chore:` 维护性工作

一次提交尽量只表达一个意图。不要把格式化、生成产物和业务逻辑混成难以审查的大提交。

## 素材与许可证

只提交你有权再分发的代码、字体、图片和声音，并在需要时附上来源与许可证。当前内嵌字体适用 `licenses/OFL.txt`。项目源代码许可证尚待首次正式开源发行前确定；在此之前，请不要假定仓库内容已按某个常见开源许可证授权。
